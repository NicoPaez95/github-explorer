import { RepoCard } from "./RepoCard";
const RepoList = ({ repo }) => {
  return (
    <>
      {/* here the grid with repositories*/}
      <div>here the grid with repositories</div>
      <RepoCard repo={repo} />
    </>
  );
};

export { RepoList };
